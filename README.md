# homebrew-kiln

Homebrew tap for [kiln](https://git.exidia.com/tmk/kiln), a template compiler
for GFI (gulp-file-include) syntax.

## Install

```bash
brew install teehemkay/kiln/kiln
```

`Formula/kiln.rb` and the release-asset binaries are maintained by kiln's
`bb release` task.
